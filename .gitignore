odyssey
*.sts.yaml
!examples/verified/*.sts.yaml
.DS_Store

# Downloaded test dashboards (large; use `make download-testdata`)
testdata/dashboards/*.json

# Frontend dependencies (install via `cd web && npm install`)
web/node_modules/

# Azure deployment secrets
deploy/azure/.env
